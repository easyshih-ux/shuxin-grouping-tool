# SHUXIN 探索者集結

以 React、TypeScript 與 Vite 製作的課堂學生分組工具。

## 本機啟動

```bash
pnpm install
pnpm dev
```

## 正式建置

```bash
pnpm build
```

建置成果位於 `dist/`。

## GitHub Pages

建議使用 GitHub Actions 執行 `pnpm build`，並將 `dist/` 部署至 GitHub Pages。
